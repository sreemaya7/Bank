FROM eclipse-temurin:17-jdk

WORKDIR /app

# Copy everything from the repo
COPY . .

# Compile the server using the JAR already in the repo
RUN javac -cp sqlite-jdbc-3.45.3.0.jar BankingServer.java

# Start the server
CMD ["java", "-cp", ".:sqlite-jdbc-3.45.3.0.jar", "BankingServer"]
